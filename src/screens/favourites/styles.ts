import {StyleSheet} from 'react-native';

import {colors} from '__root/src/cons';

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    lineHeight: 28,
    color: colors.GRAY_800,
  },
});

export default styles;
